/**
 * Workspace fixture for the one-world E2B composition e2e.
 *
 * The package exists to own this test's bare workspace dependency edges and
 * keep them out of the production E2B package graph.
 *
 * @module @deepseek-ai/dsh-e2b-composition
 */

export {}
