# dependencies
node_modules/
.pnpm-store/

# lockfiles are not committed: this bundle has no runtime dependencies
package-lock.json
pnpm-lock.yaml
yarn.lock

# npm pack output
*.tgz

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# local vision toolkit artifacts
.dsh-vision-toolkit/

# NOTE: lib/client.js is a prebuilt ModuleLoader artifact. In this checkout it
# is generated by the repository-root `pnpm run build:third-party`, so the
# root .gitignore's `lib/` rule already ignores it and it is never committed.
